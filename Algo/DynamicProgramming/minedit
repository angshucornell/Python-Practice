def subcost(x,y):
	if (x == y ):
		return 0
	else :
		return 2

def insertCost(x):
	return 1
def deleteCost(x):
	return 1

def minedit(source, target):
	n = len(source)
	m = len(target)

	ans= [ [0] * (m+1) for i in range ( 1, n+1) ]

	for i in 
	
	
